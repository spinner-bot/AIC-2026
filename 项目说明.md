# 项目说明

> README.md 保留团队宣言，本文件为算法4 的开发与提交说明。

## 一、项目简介

**AIC-2026 算法挑战赛 · 算法4：面向噪声标签数据的细粒度图像识别鲁棒微调**

| 项 | 内容 |
|---|---|
| 任务 | 自然动植物细粒度图像分类（单标签） |
| 骨干 | CLIP ViT-B/32（强制，OpenAI 官方权重） |
| 指标 | Top-1 Accuracy；线上综合分 = 复赛 40% + 半决赛 60% |
| 主攻 | CoOp + NOPD（Noisy Student On-Policy Self-Distillation）+ 鲁棒损失 + WiSE-FT |

## 二、目录结构

```
aic-2026/
├── README.md            # 团队宣言
├── 项目说明.md          # 本文件：开发/提交说明
├── requirements.txt     # 环境依赖
├── .gitignore
├── status.md            # 项目进度
├── CP statement/        # 赛题说明
│   └── 算法4-噪声图像识别/
├── docs/                # 技术路线等文档
├── specs/               # 团队规范
├── configs/             # 超参配置（yaml）
├── src/                 # 核心代码
│   ├── data/            #   Dataset/Dataloader、噪声筛选
│   ├── models/          #   CLIP 加载 + PEFT 模块
│   ├── losses/          #   GCE/SCE/ELR/MAE/NOPD 自蒸馏
│   ├── train.py         #   训练入口
│   ├── inference.py     #   推理 → pred_results.csv
│   └── utils/           #   工具
└── scripts/             # 下载/训练/评测脚本
```

## 三、环境配置

```bash
conda create -n aic python=3.10 -y
conda activate aic
pip install -r requirements.txt

# 下载 CLIP ViT-B/32 权重到 models/（首次运行一次即可）
bash scripts/download_model.sh
```

## 四、运行方法

```bash
# 训练（CoOp baseline）
python src/train.py --config configs/baseline.yaml

# 推理 + 生成提交文件
python src/inference.py --config configs/baseline.yaml --output pred_results.csv
```

## 五、提交方法

- 结果文件：`pred_results.csv`（两列：`图片文件名, 类别编号`，编号 4 位补零），压缩为 zip 提交。
- 复赛/半决赛另需：可运行代码 + README + requirements.txt + 技术报告 PDF。
